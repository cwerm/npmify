<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>NPMify</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="assets/npmify.css" />
</head>
    <body>
        <header class="header lg:px-16 px-6 bg-white flex flex-wrap items-center lg:py-0 py-2">
            <div class="flex-1 flex justify-between items-center">
                <h3><span class="outdatedCount">{{ .OutdatedCount }}</span> out of {{ .TotalDependencies }} dependencies are outdated.</h3>
            </div>
        </header>
        <div class="header hidden">
            <px-marquee data-outdated="{{ .OutdatedCount }}" data-total="{{ .TotalDependencies }}"></px-marquee>
        </div>

        <div class="filters">
            <ul class="list-reset flex lg:px-16 px-6 py-4">
                <li class="mr-3">
                    <button data-filter="outdated" class="filter inline-block border rounded hover:border-gray-lighter text-blue hover:bg-gray-300 py-1 px-3">Remove Outdated</button>
                </li>
                <li class="mr-3">
                    <button data-filter="current" class="filter inline-block border rounded hover:border-gray-lighter text-blue hover:bg-gray-300 py-1 px-3">Remove Current</button>
                </li>
                <li class="mr-3">
                    <button data-filter="reset" class="filter inline-block border rounded hover:border-red-700 hover:bg-red-500 hover:text-white py-1 px-3">Reset</button>
                </li>
            </ul>

        </div>
        <div class="container mx-auto">
            <div class="bg-white mx-auto max-w-lg shadow-lg rounded-lg overflow-hidden">
                <div class="flex items-center py-4">
                    <div class="flex-grow">
                        <div class="flex border-b">
                            <h3 class="font-bold text-xl px-4 py-3 leading-tight">Dependencies</h3>
                            <div class="flex justify-end w-full">
                                <label for="searchFilter">
                                    <input type="search" name="serchFilter" placeholder="Filter" id="searchFilter" class="bg-white h-10 px-5 pr-10 border rounded-full text-sm focus:outline-none" />
                                </label>
                            </div>
                        </div>
                        <div class="w-full">

                            <div id="list"></div>

                        </div>
                    </div>
                </div>
            </div>
        </div>

        <button class="makeFancy btn">🎉</button>

        <script src="assets/npmify.js"></script>
    </body>
</html>